# المخطط الفني والتجاري لنظام رامسسكو (Ramssko Lab ERP/LIMS)
## الجزء الثالث: دورات عمل الكيانات ودورات الحياة
**الملف:** `02_Business_Workflows_and_Lifecycles.md`

---

### 1. دورة حياة الجسة الاستكشافية (Borehole Lifecycle)

تبدأ دورة حياة الجسة الاستكشافية من الاتصال الأولي بالعميل وتنتهي بتسليم التقرير الجيوتقني وإغلاق المعاملة مالياً، وتتكون من المراحل المتسلسلة التالية:

```mermaid
flowchart TD
    %% Define Styles for visual excellence
    classDef stageStyle fill:#0f172a,stroke:#3b82f6,stroke-width:2px,color:#60a5fa,font-weight:bold;
    classDef stepStyle fill:#1e293b,stroke:#475569,stroke-width:1.5px,color:#cbd5e1;
    
    subgraph Stage1 ["المرحلة الأولى: الاتصال والتعاقد المالي"]
        direction LR
        A["استفسار العميل المبدئي"]:::stepStyle --> B["حساب التسعير تلقائياً بالنظام"]:::stepStyle
        B --> C["إصدار عرض السعر برقم مرجع"]:::stepStyle
        C --> D["الموافقة والتوقيع الإلكتروني للعميل"]:::stepStyle
        D --> E["تحصيل دفعة التأمين المقدمة 50%"]:::stepStyle
    end
    
    subgraph Stage2 ["المرحلة الثانية: التجهيز والتنفيذ الميداني"]
        direction LR
        F["توليد أمر الشغل WO"]:::stepStyle --> G["تجهيز الموقع وجدولة الفرق"]:::stepStyle
        G --> H["حفر الجسات واستخراج العينات"]:::stepStyle
        H --> I["التصوير الميداني وتوثيق الـ GPS"]:::stepStyle
    end
    
    subgraph Stage3 ["المرحلة الثالثة: الفحص والاختبار المعملي"]
        direction LR
        J["شحن وإرسال العينات للمعمل"]:::stepStyle --> K["إجراء الاختبارات والتحليل الفني"]:::stepStyle
        K --> L["تسجيل القراءات في شيت الاختبارات"]:::stepStyle
        L --> M["المراجعة الفنية والاعتماد الهندسي"]:::stepStyle
    end
    
    subgraph Stage4 ["المرحلة الرابعة: الإنهاء المالي والتسليم"]
        direction LR
        N["الفوترة وتسوية التكاليف الفعلية"]:::stepStyle --> O["تحصيل الدفعة المتبقية 50%"]:::stepStyle
        O --> P["إرسال التقرير النهائي المعتمد"]:::stepStyle
        P --> Q["التوثيق الجغرافي برابط Google Maps"]:::stepStyle
    end

    %% Inter-stage connections
    E --> F
    I --> J
    M --> N
    
    %% Apply general graph styles
    style Stage1 fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Stage2 fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Stage3 fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Stage4 fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
```

1. **الاستفسار والتقييم (Inquiry & Assessment):**
   * يقوم مهندس المبيعات بإدخال مواصفات المبنى (نوع المبنى: سكني/تجاري، مساحة الأرض الكلية، وعدد الأدوار).
   * يقوم النظام تلقائياً بتحديد عدد الجسات المطلوبة كحد أدنى بناءً على كود البناء السعودي (مثال: مساحة 200-300 متر مربع تتطلب 3 جسات كحد أدنى).

2. **عرض السعر المالي (Quotation Stage):**
   * يُنشئ النظام عرض سعر رسمي برقم مرجعي فريد يتخذ التنسيق التلقائي (مثال: `RAM-D-F-2606153`).
   * يتم تسعير الجسة أو سعر المتر بناءً على نطاق عمل البلدية المعتمدة (الرياض أو الدمام) وعمق الجسات المطلوب.
   * يُرسل عرض السعر تلقائياً للعميل عبر البريد الإلكتروني أو الواتساب، وتتحول حالة المعاملة في لوحة التحكم إلى "معلقة" مع توليد تذكرة (Ticket) ملونة.

3. **الاعتماد والتوقيع (Customer Approval & Signing):**
   * يعتمد العميل عرض السعر برفع توقيعه الإلكتروني الممسوح ضوئياً (Scanned Electronic Signature) عبر بوابة العميل الإلكترونية.

4. **التسوية والتحصيل الأولي (Upfront Payment & Validation):**
   * يقوم العميل بسداد **50% كدفعة مقدمة / تأمين** (عبر السداد الإلكتروني أو تحويل بنكي مع رفع إيصال الدفع).
   * يراجع محاسب المبيعات الدفعة، ويعتمدها بالنظام، وتتحول حالة المعاملة إلى **"معاملة جاهزة"**.

5. **الجدولة والتنفيذ الميداني (Field Scheduling & Execution):**
   * يحول النظام المعاملة لقسم التشغيل، ويصدر مدير التشغيل أمر الشغل (Work Order) وتعيين فريق الحفر والجيولوجي.
   * يتم استلام صور الموقع الجغرافي، وتحديد جدول التنفيذ الزمني، وانتقال المركبات والمعدات.
   * يقوم فني الحفر ميدانياً بحفر الجسة الفعلي وتوثيق أعماقها وأخذ العينات الممثلة.
   * يقوم المهندس الجيولوجي بتوثيق إحداثيات الجسة الجغرافية بالـ GPS، ووقت أخذ العينة، وتصوير عينات التربة الميدانية.

6. **الاختبارات المعملية والتقارير (Lab Testing & Review):**
   * تُرسل عينات التربة إلى المعمل تحت إشراف المهندس المختص.
   * يوزع مدير المختبر العينات على فنيي المعمل لإجراء فحوصات التصنيف والدمك وحدود اتربرج.
   * يتم تسجيل الأوزان والنتائج في "شيت الاختبارات"، وتدقيقها من قبل المراجع الفني للتأكد من مطابقتها لكود التربة المعتمد.
   * يُصدر الجيولوجي والمدير الفني التقرير النهائي (تقرير الجسات BH Report) المحتوي على التوصيات وقدرة التحمل.

7. **الإنهاء المالي والتسليم (Financial Closure & Delivery):**
   * يحال الملف للحسابات، ويتم مراجعة التكاليف الفعلية مقابل المقدرة، وإجراء التعديلات إن وجدت.
   * يُطلب من العميل سداد الـ 50% المتبقية من قيمة المشروع.
   * فور إتمام السداد، يتم إصدار الفاتورة الضريبية النهائية والربط مع ZATCA و Microtek.
   * يُرسل التقرير المعتمد النهائي للعميل بالوسائل المتاحة، ويرفق تقييم الموقع عبر خرائط Google Maps في سجل المعاملة بالنظام.

---

### 2. دورة حياة اختبارات الخرسانة (Concrete Testing Lifecycle)

تنظم هذه الدورة عمليات استلام وحفظ واختبار مكعبات الخرسانة الواردة من المواقع الإنشائية للتحقق من كفاءتها ومطابقتها للمواصفات:

```mermaid
flowchart TD
    %% Define Styles
    classDef stageStyle fill:#0f172a,stroke:#3b82f6,stroke-width:2px,color:#60a5fa,font-weight:bold;
    classDef stepStyle fill:#1e293b,stroke:#475569,stroke-width:1.5px,color:#cbd5e1;
    classDef failStyle fill:#450a0a,stroke:#ef4444,stroke-width:1.5px,color:#fca5a5;

    subgraph Prep ["مرحلة التحضير الميداني والصب"]
        direction LR
        A["أخذ عينات الخرسانة بالموقع\n(6 لـ 100م³ أولى + 7 لكل 250م³ إضافي)"]:::stepStyle --> B["إجراء اختبار الهبوط (Slump Test) ميدانياً"]:::stepStyle
        B --> C["تدوين القراءات في سجل ملاحظات صب الخرسانة"]:::stepStyle
    end

    subgraph Curing ["مرحلة الاستلام والمعالجة المعملية"]
        direction LR
        D["استلام المكعبات بالمعمل وترميزها تسلسلياً"]:::stepStyle --> E["غمر المكعبات في حوض المعالجة المائي المخصص"]:::stepStyle
    end

    subgraph Testing ["مرحلة اختبارات الكسر والتكسير"]
        direction TD
        E --> F["كسر أول 6 عينات بعد 7 أيام من الصب"]:::stepStyle
        E --> G["كسر عينات المقاومة النهائية بعد 28 يوماً"]:::stepStyle
        F & G --> H["تسجيل قياسات الوزن والأبعاد ودخول شيت الاختبارات"]:::stepStyle
    end

    subgraph Eval ["مرحلة التقييم وإصدار التقرير"]
        direction TD
        H --> I{"تقييم نسبة النجاح\n(المقاومة الفعالية >= 75%)"}
        
        I -- نعم [مطابقة] --> J["اعتماد القراءات وإصدار التقرير النهائي المعتمد"]:::stepStyle
        I -- لا [غير مطابقة] --> K["إطلاق تنبيه جودة الجودة وإخطار المدير الفني"]:::failStyle
        
        K --> L["التوجيه لإجراء اختبارات تكميلية غير متلفة\n(Schmidt Hammer / Core Test)"]:::stepStyle
        L --> J
    end

    %% Flow connections between stages
    C --> D
    D --> E
    
    %% Style Subgraphs
    style Prep fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Curing fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Testing fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
    style Eval fill:#0f172a,stroke:#1e3a8a,stroke-width:1.5px,color:#93c5fd;
```

1. **أخذ العينات الميدانية (Site Sampling):**
   * يتم أخذ العينات بناءً على حجم صب الخرسانة في الموقع: **6 عينات لحجم 100م³ الأولى**، ويضاف **7 عينات إضافية لكل 250م³ إضافي**.
   * يتم ترميز العينات وكتابة بياناتها: (الرقم التسلسلي، مكان الصب بالتحديد، وتاريخ الصب).

2. **اختبار الهبوط (Slump Test):**
   * يجرى اختبار الهبوط ميدانياً للخرسانة الرطبة قبل الصب، وتسجل الملاحظات والقيم فوراً في سجل "ملاحظات صب الخرسانة".

3. **الاستلام والمعالجة (Receiving & Curing):**
   * تُنقل المكعبات الخرسانية إلى المعمل، وتوضع في حوض المعالجة المائي المخصص للتحكم في رطوبتها وتصلدها الطبيعي.

4. **اختبار التكسير (Crushing Tests):**
   * **مرحلة الـ 7 أيام:** يتم سحب أول **6 عينات** من حوض المعالجة بعد مرور 7 أيام من تاريخ الصب، وقياس وزنها وأبعادها (الطول، العرض، الارتفاع) بدقة، ثم تكسيرها بمكبس هيدروليكي لتسجيل مقاومة الضغط الأولية في "شيت الاختبارات".
   * **مرحلة الـ 28 يوماً:** يتم سحب واختبار العينات المتبقية بعد مرور 28 يوماً للحصول على القوة التصميمية النهائية للخرسانة.

5. **تقييم ومطابقة النتائج (Evaluation & Compliance):**
   * يحتسب النظام تلقائياً مقاومة التكسير، ويشترط لنجاح الاختبار ألا تقل النتيجة عن **75% كحد أدنى** من القوة التصميمية المطلوبة.
   * **إجراءات النتائج غير المطابقة:** في حال فشل العينة (أقل من 75%)، يطلق النظام تنبيهاً بالجودة للجوانب الإدارية والفنية، ويوصي النظام بإجراء اختبارات غير متلفة إضافية مثل **مطرقة شميدت (Schmidt Hammer)** أو سحب عينة قلب خرساني **(Core Test)** للتحقق من صلب العنصر الإنشائي المشكوك فيه بالموقع.

---

### 3. مخطط التتابع التشغيلي للجسات (Borehole Sequence Diagram)

يوضح المخطط التالي تتابع نداءات العمليات والبيانات بين جميع أطراف النظام والموديولات المنفصلة لتنفيذ معاملة الجسات من البداية وحتى الإغلاق:

```mermaid
sequenceDiagram
    autonumber
    actor Client as العميل (Client)
    actor Sales as مهندس المبيعات
    participant CRM as موديول علاقات العملاء (CRM)
    participant Ticket as موديول التذاكر (Ticketing)
    participant Finance as موديول المالية (Finance)
    participant Ops as موديول التشغيل (Operations)
    participant Field as المهندس الجيولوجي / الحفار
    participant Lab as إدارة المختبر (Lab)

    %% Step 1: Initial contact & quote
    Client->>Sales: طلب خدمة فحص تربة (تحديد المساحة والأدوار والموقع)
    Sales->>CRM: تسجيل الطلب وحساب عدد الجسات والعمق تلقائياً
    CRM->>CRM: توليد عرض السعر بالمرجع (RAM-D-F-2606153)
    CRM->>Client: إرسال عرض السعر للعميل (Email / WhatsApp)
    
    %% Step 2: Customer Approval
    Client->>Sales: إرسال الموافقة ورفع التوقيع الإلكتروني الممسوح
    Sales->>CRM: إرفاق التوقيع الإلكتروني وتغيير حالة العرض إلى "مقبول"
    
    %% Step 3: Finance Verification
    CRM->>Ticket: إنشاء تذكرة معاملة بحالة "معلقة (Pending)" ملونة
    CRM->>Finance: إرسال بيانات الفاتورة الأولية
    Client->>Finance: سداد 50% دفعة مقدمة (بوابة الدفع أو تحويل بنكي)
    Finance->>Finance: التحقق من الدفع وتطبيق التوجيه المحاسبي المالي
    
    %% Step 4: Operations & Field Work
    Finance->>Ticket: تحديث حالة التذكرة إلى "تحت التنفيذ"
    Finance->>Ops: إصدار أمر الشغل (Work Order)
    Ops->>Field: تكليف طاقم الحفار والجيولوجي بالموقع والجدولة الزمنية
    Field->>Field: فحص السيارة وتأكيد الاستلام عبر تطبيق الهاتف
    Field->>Ops: رفع إحداثيات الجسات الميدانية (GPS) وصور العينات المستخرجة
    Field->>Lab: شحن وتسليم العينات الميدانية رسمياً
    
    %% Step 5: Lab Work & Reporting
    Lab->>Lab: استلام وتوزيع عينات الجسات وإجراء اختبارات التصنيف والدمك
    Lab->>Ops: إدخال النتائج في شيت الاختبارات وتدقيقها وإصدار مسودة التقرير الجيوتقني
    Ops->>Ops: مراجعة التقرير واعتماده فنياً من المدير الفني والجيوتقني
    Ops->>Finance: تحويل الملف المكتمل لتسوية التكاليف
    
    %% Step 6: Final Payment and Delivery
    Finance->>Finance: حساب الفروقات المالية وإصدار الفاتورة النهائية والربط مع ZATCA و Microtek
    Finance->>Client: إشعار بطلب سداد الـ 50% المتبقية
    Client->>Finance: سداد المبلغ المتبقي بالكامل
    Finance->>Ticket: تحديث حالة التذكرة إلى "معتمدة (Approved)" وإغلاقها
    Ops->>Client: تسليم التقرير الجيوتقني النهائي المعتمد + إرفاق تقييم Google Maps
```

---

### 4. مخطط التتابع لاختبار الكثافة الحقلية (FDT Sequence Diagram)

يوضح المخطط التالي دورة العمل الفنية الميدانية لاختبار الكثافة الحقلية للتربة (FDT) وآلية معالجة النتائج المطابقة وغير المطابقة لمعايير الجودة:

```mermaid
sequenceDiagram
    autonumber
    actor Consultant as المهندس الاستشاري بالموقع
    participant LabTech as فني المختبر الميداني
    participant Mobile as تطبيق الهاتف المحمول (Mobile App)
    participant Ops as موديول التشغيل والمختبر
    participant Reviewer as المهندس المراجع الفني

    Ops->>LabTech: تكليف بمهمة إجراء اختبار الكثافة الحقلية FDT بالموقع
    LabTech->>LabTech: تنفيذ الفحص بموقع العميل بطريقة مخروط الرمل (ASTM D1556)
    LabTech->>Mobile: إدخال قراءات النقاط الميدانية والكثافة الحقلية الجافة
    LabTech->>Consultant: أخذ توقيع الاستشاري والعميل الميداني على شيت القراءات
    LabTech->>Mobile: تصوير شيت القراءات الموقع ميدانياً ورفع إحداثيات الموقع
    Mobile->>Ops: مزامنة البيانات والصور مع خادم النظام الموحد
    Ops->>Reviewer: تحويل الاختبار للمراجعة الفنية واعتماده كودياً
    Reviewer->>Reviewer: مقارنة درجة دمك التربة المسجلة بالكثافة المعملية القصوى

    alt درجة الدمك أكبر من 95% (> 95%) [مطابقة]
        Reviewer->>Ops: اعتماد الاختبار وتوليد تقرير FDT النهائي بالترميز RAM-FDT-XXXX
        Ops->>Consultant: تصدير وإرسال التقرير النهائي المعتمد للعميل والاستشاري
    else درجة الدمك أقل من أو تساوي 95% [غير مطابقة]
        Reviewer->>Ops: رفض اعتماد النتيجة وإطلاق تنبيه فشل الاختبار بالجودة
        Ops->>LabTech: إرسال تكليف إعادة اختبار وإصدار تعليمات للموقع بإعادة الدمك
        LabTech->>LabTech: إعادة زيارة الموقع بعد إعادة الدمك وإجراء الفحص مجدداً
    end
```

---

### 5. مخطط آلة الحالة للتذكرة والمعاملة (Ticket State Machine Diagram)

يتحكم النظام بحالة المعاملة والتذكرة بنظام أوتوماتيكي دقيق يربط بين الموافقة المالية والعمليات التشغيلية، ويوضح مخطط آلة الحالة التالي الانتقالات بين الحالات التشغيلية الخمس المعتمدة بالنظام:

```mermaid
stateDiagram-v2
    [*] --> معلقة : إعداد عرض السعر الأولي (رقم RAM-D-F-XXXX) تلقائياً
    
    state "معلقة (Pending)" as معلقة
    state "تحت التنفيذ (In Progress)" as تحت_التنفيذ
    state "قيد الانتظار (On Hold)" as قيد_الانتظار
    state "جاري العمل عليها (Working)" as جاري_العمل
    state "معتمدة ومغلقة (Approved)" as معتمدة

    معلقة --> تحت_التنفيذ : سداد الدفعة المقدمة (50%) وإصدار أمر الشغل (Work Order)
    معلقة --> [*] : إلغاء المعاملة أو عدم موافقة العميل على عرض السعر

    تحت_التنفيذ --> قيد_الانتظار : توقف العمل الميداني (عدم جاهزية الموقع / عوامل مناخية / تعديل في نطاق العقد)
    قيد_الانتظار --> تحت_التنفيذ : حل المعوقات الميدانية واستئناف أعمال الحفر

    تحت_التنفيذ --> جاري_العمل : الانتهاء الميداني للجسات ونقل العينات للمختبر لبدء الفحص المعملي
    جاري_العمل --> قيد_الانتظار : ظهور نتائج غير مطابقة (مثل فشل الدمك أو الخرسانة) تستدعي الفحص الاستثنائي
    
    جاري_العمل --> معتمدة : الانتهاء من إعداد ومراجعة التقرير الفني + سداد الـ 50% المتبقية مالياً
    معتمدة --> [*] : تسليم التقارير المعتمدة للعميل وتوثيق Google Maps وإغلاق التذكرة نهائياً
```
